# 词汇合并功能实现文档

## 功能概述

本功能实现了合并同一个单词在多个句子或例题中出现的情况，将所有例句整合到同一个单词条目中，提供更丰富的学习体验。

## 核心特性

### 1. 词汇合并 (Vocabulary Merging)
- **自动检测重复单词**：系统会自动检测相同单词在不同句子中的出现
- **智能合并**：将相同单词的所有例句、翻译和定义合并到一个条目中
- **保持原有展示**：在main页面中保持按句子展示的方式不变
- **多例句展示**：在单词卡片中可以查看该单词的所有例句

### 2. 数据结构改进

#### 原有结构
```javascript
learnedItems: [
  {
    originalText: "句子1",
    translation: "翻译1", 
    selectedWords: [
      { word: "word1", translation: "翻译", definition: "定义" }
    ],
    timestamp: "时间戳"
  }
]
```

#### 新增合并结构
```javascript
mergedVocabulary: {
  "word1": {
    word: "word1",
    definition: "主要定义",
    translation: "主要翻译",
    examples: [
      {
        originalText: "句子1",
        translation: "翻译1",
        timestamp: "时间戳1",
        contextTranslation: "语境翻译1",
        definition: "语境定义1"
      },
      {
        originalText: "句子2", 
        translation: "翻译2",
        timestamp: "时间戳2",
        contextTranslation: "语境翻译2",
        definition: "语境定义2"
      }
    ],
    firstTimestamp: "第一次出现时间",
    totalOccurrences: 2
  }
}
```

### 3. 处理流程

#### 3.1 初始化合并
```javascript
// 在页面加载时，系统会：
1. 检查是否已有合并词汇表
2. 如果没有，自动合并现有词库
3. 保存合并结果到storage
4. 更新显示计数器
```

#### 3.2 新单词添加处理
```javascript
// 当用户添加新单词时：
1. 检查该单词是否已存在于合并词汇表中
2. 如果存在，将新例句添加到现有条目
3. 如果不存在，创建新的词汇条目
4. 更新存储和显示
```

#### 3.3 界面展示逻辑
```javascript
// 在main页面：
1. 保持原有的按句子展示方式
2. 单词chip显示例句数量（如有多个）
3. 添加视觉标识区分有多个例句的单词
4. 点击单词打开对应的卡片类型

// 在单词卡片：
1. 检测是否为合并单词
2. 如果是合并单词，显示所有例句
3. 如果不是，显示传统单一语境卡片
```

## 文件修改清单

### 1. 新增文件
- **`scripts/vocabulary-merger.js`** - 核心合并逻辑类
- **`test_vocabulary_merger.html`** - 功能测试页面

### 2. 修改的现有文件

#### 2.1 配置文件
- **`manifest.json`** - 添加vocabulary-merger.js到content scripts
- **`views/main.html`** - 添加vocabulary-merger.js脚本引用
- **`views/wordCard.html`** - 添加vocabulary-merger.js脚本引用
- **`views/review.html`** - 添加vocabulary-merger.js脚本引用

#### 2.2 核心逻辑文件
- **`scripts/main.js`** - 添加合并初始化逻辑和界面更新
- **`scripts/content.js`** - 修改保存逻辑使用合并功能
- **`scripts/wordCard.js`** - 添加合并单词卡片显示逻辑

#### 2.3 样式文件
- **`styles/main.css`** - 添加合并单词的视觉样式
- **`styles/wordCard.css`** - 添加多例句展示样式

## 主要功能说明

### 1. VocabularyMerger类

#### 核心方法：

**`mergeExistingVocabulary(learnedItems)`**
- 合并现有词库中的重复单词
- 返回重构后的学习项目数组
- 建立完整的合并词汇表

**`processNewWords(newItem, existingItems)`** 
- 处理新加入的单词
- 检查与现有词汇的重复情况
- 返回更新后的项目数组和合并信息

**`getWordData(word)`**
- 获取单词的完整合并信息
- 包含所有例句和语境

**`getStatistics()`**
- 提供词汇统计信息
- 包括总数、重复数、平均例句数等

### 2. 用户界面改进

#### 2.1 主页面 (main.js/main.html)
- **视觉标识**：有多个例句的单词会显示特殊样式和数量标识
- **智能跳转**：点击单词时根据是否有多例句决定打开的卡片类型
- **保持兼容**：原有的按句子展示方式完全保留

#### 2.2 单词卡片 (wordCard.js/wordCard.html)
- **例句列表**：显示该单词的所有出现语境
- **时间排序**：按时间顺序展示例句
- **语境信息**：每个例句包含原文、翻译、语境定义
- **折叠展开**：超过3个例句时提供展开/折叠功能

### 3. 存储优化

#### 3.1 双存储结构
- **`learnedItems`**：保持原有结构，确保向后兼容
- **`mergedVocabulary`**：新增合并词汇表，优化查询性能

#### 3.2 自动同步
- 添加新单词时自动更新两个存储
- 确保数据一致性
- 支持增量更新

## 使用说明

### 1. 初次使用
系统会自动检测现有词库并进行合并，无需用户手动操作。

### 2. 日常使用
- **添加新单词**：正常使用翻译功能，系统会自动处理重复
- **查看单词**：点击单词chip查看详细信息和所有例句
- **复习功能**：复习模式会自动使用合并后的词汇

### 3. 视觉提示
- **绿色边框 + 点标识**：表示该单词有多个例句
- **数字标识**：显示该单词的总出现次数
- **悬停提示**：显示"This word appears in X sentences"

## 测试说明

使用 `test_vocabulary_merger.html` 进行功能测试：

1. **创建测试数据** - 生成包含重复单词的测试数据
2. **合并测试** - 测试现有词汇的合并功能
3. **添加新单词** - 测试新单词的添加和合并
4. **添加重复单词** - 测试重复单词的处理
5. **存储检查** - 查看存储中的数据结构
6. **统计显示** - 查看合并统计信息

## 兼容性说明

- **向后兼容**：现有功能和数据结构完全保留
- **渐进增强**：新功能是在现有基础上的增强
- **数据安全**：所有操作都保持原有数据不受影响
- **性能优化**：通过合并词汇表提高查询和显示性能

## 未来扩展

1. **智能定义选择**：基于频率和长度选择最佳定义
2. **例句排序优化**：支持按重要性或难度排序
3. **批量操作**：支持批量合并和管理重复词汇
4. **统计分析**：提供更详细的学习统计和分析
5. **导出功能**：支持导出合并后的词汇表

## 注意事项

1. **首次初始化**：系统首次加载时会进行一次性的词汇合并，可能需要几秒时间
2. **存储空间**：合并后会占用额外存储空间来保存合并词汇表
3. **性能考虑**：大量词汇时合并操作可能较慢，但只在初始化时进行
4. **数据一致性**：建议定期使用测试页面验证数据一致性
